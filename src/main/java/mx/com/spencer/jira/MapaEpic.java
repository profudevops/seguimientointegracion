package mx.com.spencer.jira;

public class MapaEpic {

	public String getEpic(String epica) {
		String respuesta = "";
		if(epica.trim().toUpperCase(). equals("ALUX")) {
			respuesta = "INPROD-1";
			return respuesta; 
		}
		if(epica.trim().toUpperCase(). equals("FINDUR")) {
			respuesta = "INPROD-2";
			return respuesta; 
		}
		if(epica.trim().toUpperCase(). equals("NIK")) {
			respuesta = "INPROD-3";
			return respuesta; 
		}
		if(epica.trim().toUpperCase(). equals("TELLWARE WEB")) {
			respuesta = "INPROD-4";
			return respuesta; 
		}
		if(epica.trim().toUpperCase(). equals("NCI MONITOR")) {
			respuesta = "INPROD-5";
			return respuesta; 
		}
		if(epica.trim().toUpperCase(). equals("OP RETIROS")) {
			respuesta = "INPROD-6";
			return respuesta; 
		}
		if(epica.trim().toUpperCase(). equals("ANFEXI ALERTA PENSIONES")) {
			respuesta = "INPROD-7";
			return respuesta; 
		}
		if(epica.trim().toUpperCase(). equals("ANFEXI ALERTA")) {
			respuesta = "INPROD-8";
			return respuesta; 
		}
		if(epica.trim().toUpperCase(). equals("BRANCH OFFICE")) {
			respuesta = "INPROD-9";
			return respuesta; 
		}
		if(epica.trim().toUpperCase(). equals("ESTADO DE CUENTA MIT")) {
			respuesta = "INPROD-10";
			return respuesta; 
		}
		if(epica.trim().toUpperCase(). equals("AFORE MOVIL")) {
			respuesta = "INPROD-11";
			return respuesta; 
		}
		if(epica.trim().toUpperCase(). equals("AUDITORIA DE SALDOS Y MOVIMIENTOS")) {
			respuesta = "INPROD-12";
			return respuesta; 
		}
		if(epica.trim().toUpperCase(). equals("IMPULSA-T DIGITAL")) {
			respuesta = "INPROD-13";
			return respuesta; 
		}
		if(epica.trim().toUpperCase(). equals("PRODUCE ASESORA")) {
			respuesta = "INPROD-14";
			return respuesta; 
		}
		if(epica.trim().toUpperCase(). equals("PRODUCE REGISTRO")) {
			respuesta = "INPROD-15";
			return respuesta; 
		}
		if(epica.trim().toUpperCase(). equals("SISTEMA DE RETIROS (SISTEMA DE TRAMITES DE PAGOS Y BENEFICIOS)")) {
			respuesta = "INPROD-16";
			return respuesta; 
		}
		if(epica.trim().toUpperCase(). equals("ENLACE FINANCIERO LEGC")) {
			respuesta = "INPROD-17";
			return respuesta; 
		}
	
		if(epica.trim().toUpperCase(). equals("PORTAL CORPORATIVO")) {
			respuesta = "INPROD-19";
			return respuesta; 
		}
	
		if(epica.trim().toUpperCase(). equals("PRODUCE REGISTRO")) {
			respuesta = "INPROD-21";
			return respuesta; 
		}
		if(epica.trim().toUpperCase(). equals("PEOPLE SOFT 91")) {
			respuesta = "INPROD-22";
			return respuesta; 
		}
		
		
		
		
		return respuesta; 
	}
	
}
